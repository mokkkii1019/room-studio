# Room Studio ローカル補完サーバー用
fastapi>=0.110
uvicorn[standard]>=0.29
pillow>=10.0
numpy>=1.24
onnxruntime>=1.17          # /bgcut（AI背景切り抜き・ISNet）
simple-lama-inpainting>=0.1.2
# 注: GPUが無いPCでは、容量の小さいCPU版torchを先に入れると快適です:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
